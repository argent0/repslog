**Requirements Document: Extending `repslog` with RIR ("Reps Before Failure") and Effective Reps Training Support**

You are an expert Rust developer building production-grade CLI tools. You are now extending the existing `repslog` application (the Rust + SQLite CLI workout tracker already built from the previous specification).

**Project Context (the haystack)**  
`repslog` is a serious, scriptable, non-interactive-first tool used by real lifters and automated LLM-based input pipelines to log accurate training data. It must remain extremely flexible for every training style, including traditional sets, calisthenics, cardio, and now **effective-reps / rest-pause / myo-reps style hypertrophy training**.

The user wants first-class, high-quality support for:
- Tracking **RIR** ("reps before failure" / Reps In Reserve) — the number of additional perfect reps the lifter believes they could have performed before true muscular failure.
- Tracking **effective reps** — the number of truly stimulating/hypertrophy-effective repetitions performed in a set (or rest-pause cluster).
- Comfortably logging complex rest-pause / myo-reps sequences, such as:  
  "I did 10 dips to failure, rested 15 seconds, then did 5 more, rested 15 seconds, then did 5 more."

All new functionality must be added while preserving 100% backward compatibility with existing workouts and data.

**Strict Non-Negotiable Rules (the needle – follow exactly)**  
This is **not** a toy project. The tool is used to store real, long-term user training data, including input generated by LLMs and shell scripts. Therefore:

- **Never use defaults** for any semantically meaningful argument (reps, weight_kg, rir, effective_reps, rest_seconds, duration_seconds, rpe, notes, etc.).  
- Every critical field that affects the meaning of the data must be **explicitly supplied** by the user on the command line.  
- If a required argument is missing, the command must **fail immediately** with a clear, actionable error message that explains exactly what is missing and how to fix it.  
- All new flags must be long-form only (`--rir`, `--effective-reps`, etc.) and have excellent help text + examples.  
- The CLI must remain fully pipeable and non-interactive friendly (e.g. `echo "42" | repslog set add --rir 0 --effective-reps 8 ...`).

**Required New Capabilities**

1. **RIR Tracking ("reps before failure")**  
   - Allow logging the number of reps the lifter estimates they had left in reserve at the end of the set (float value, e.g. `0.0` = true failure, `2.0` = two reps left, `1.5` = one-and-a-half, etc.).  
   - This must be a first-class, queryable column (not hidden in JSON).

2. **Effective Reps Tracking**  
   - Allow logging the number of effective/stimulating reps for the set or rest-pause cluster (integer).  
   - This is independent of raw `reps` (a user may record raw reps = 10 but effective_reps = 5).

3. **Rest-Pause / Myo-reps / Cluster Set Support**  
   - The system must make it natural and clear to log sequences such as:  
     10 dips to failure (RIR=0, effective_reps=6) → rest 15s → 5 more (RIR=0, effective_reps=4) → rest 15s → 5 more (RIR=1, effective_reps=3).  
   - The agent may choose the cleanest implementation that achieves excellent UX:  
     - Multiple related `exercise_sets` rows grouped by a new `cluster_id` / `rest_pause_group` (recommended for flexibility), or  
     - A new optional `is_rest_pause_continuation` flag + `rest_seconds_from_previous`, or  
     - Any other elegant, normalized design that keeps queries and output readable.  
   - Whichever approach is chosen, the `set list` and `workout view` commands must display rest-pause clusters in a visually clear, grouped way (e.g. indented sub-rows or a "cluster" label).

4. **CLI Changes (only add / extend – do not remove or rename existing commands)**  
   - Extend `repslog set add` with new explicit flags:  
     `--rir <float>`  
     `--effective-reps <int>`  
     (These must be required in the sense that the command fails helpfully if the user intends to use effective-reps mode but omits them.)  
   - Add a new convenience command `repslog set add-rest-pause` (or similar) that makes logging a full cluster sequence ergonomic while still requiring every value explicitly.  
   - Update `set list`, `workout-exercise list`, `workout view`, and any stats commands to display the new RIR and effective_reps columns in tables (using your existing pretty-printing library).  
   - Add clear examples in help text showing both a normal set and a rest-pause cluster.

5. **Database & Migration**  
   - Add the new fields via a new, versioned migration file (e.g. `03_add_rir_effective_reps_support.sql`).  
   - Keep the schema clean, normalized, and performant.  
   - All existing data must continue to work unchanged (new columns nullable).  
   - The agent decides the exact column names and any additional grouping table if needed, but the solution must be robust and easily extensible.

6. **Output & Usability**  
   - Tables must clearly show RIR (e.g. "RIR 0.0" or "to failure") and effective reps.  
   - When displaying a rest-pause cluster, the output should make the sequence obvious to the user at a glance.  
   - All error messages must be user-friendly and actionable.

**Final Instructions**  
Implement these new features on top of the existing codebase. Produce the complete updated files (new migration, updated models, updated CLI definitions, updated command handlers, etc.) with the same high quality, idiomatic Rust, excellent error handling, and clean architecture used in the original implementation.

Do not introduce any default values for the new data fields. Prioritize explicitness and data integrity above all else. The resulting tool must feel professional enough for a serious lifter (or LLM pipeline) to trust with years of training data.

Begin implementation now.

