The output of `repslog workout create --help` doesn't let the user (usually a
llm) understand how to properly store different kinds of workouts.

In particular how to store runs, but also other types of workouts.

Does `repslog` restricts the possible values of workout type?

Consider the sample database `repslog.db` and detect data entry issues. In
particular, not making full use of `repslog` facilities.

Write a report to the `reports` folder under the current name.
